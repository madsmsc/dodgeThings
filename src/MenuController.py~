"""
lav en singleton klasse som har _instance/getInstace() logikken
lad alle controllers arve fra den og fjern deres singleton logik
lav en setup metode paa singleton
soerg for at singleton markeres abstrakt og det samme goeres for setup metoden
hvilket goer at den SKAL implementeres
"""


class MenuController:
    
    class Menu(Enum):
        NO = 0,
        MAIN = 1,
        SETTINGS = 2

    menu: Menu = Menu.NO

    def isShowMenu() -> Bool:
        return self.menu != self.Menu.NO

    def hideMenu():
        self.menu = self.Menu.NO

    def showMainMenu():
        self.menu = self.Menu.MAIN

    def showSettingsMenu():
        self.menu = self.Menu.SETTINGS

    def show
